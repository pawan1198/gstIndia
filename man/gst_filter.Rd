% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{gst_filter}
\alias{gst_filter}
\title{Filter GST State-wise Data}
\usage{
gst_filter(
  financial_year = NULL,
  state = NULL,
  region = NULL,
  component = NULL,
  from_month = NULL,
  to_month = NULL
)
}
\arguments{
\item{financial_year}{\code{character} vector of financial years to keep, e.g.
\code{c("2022-23", "2023-24")}. \code{NULL} (default) returns all years.}

\item{state}{\code{character} vector of state/UT names to keep. \code{NULL} returns
all states. Partial matching is \strong{not} performed; use exact names as in
\link{gst_statewise}.}

\item{region}{\code{character} vector of region labels (\code{"North"}, \code{"South"},
\code{"East"}, \code{"West"}, \code{"Central"}, \code{"Northeast"}, \code{"Other"}).
\code{NULL} returns all regions.}

\item{component}{\code{character} vector of components to return. One or more of
\code{"cgst"}, \code{"sgst"}, \code{"igst"}, \code{"cess"}, \code{"total"}. Default \code{NULL}
returns all components.}

\item{from_month}{\code{character} start month \code{"YYYY-MM"} (inclusive). Default
\code{NULL} (no lower bound).}

\item{to_month}{\code{character} end month \code{"YYYY-MM"} (inclusive). Default
\code{NULL} (no upper bound).}
}
\value{
A \code{data.table} with columns \code{financial_year}, \code{month}, \code{month_date},
\code{state_code}, \code{state}, \code{region}, and the selected components.
}
\description{
Convenience wrapper to subset \link{gst_statewise} by financial year, state,
region, and/or GST component. Returns a \code{data.table}.
}
\examples{
# All states, last two financial years
gst_filter(financial_year = c("2023-24", "2024-25"))

# Punjab and Haryana, CGST + TOTAL only
gst_filter(state = c("Punjab", "Haryana"), component = c("cgst", "total"))

# Southern region, FY 2022-23
gst_filter(region = "South", financial_year = "2022-23")

# Date range across years
gst_filter(from_month = "2023-04", to_month = "2024-03")
}
