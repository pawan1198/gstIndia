% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{gst_yoy}
\alias{gst_yoy}
\title{Year-on-Year Growth in GST Collection}
\usage{
gst_yoy(component = "total", state = NULL, financial_year = NULL)
}
\arguments{
\item{component}{\code{character(1)}. Which component to use for growth
calculation. One of \code{"cgst"}, \code{"sgst"}, \code{"igst"}, \code{"cess"}, \code{"total"}
(default).}

\item{state}{\code{character} vector to filter states. \code{NULL} (default) = all.}

\item{financial_year}{\code{character} vector to filter years. \code{NULL} = all.}
}
\value{
A \code{data.table} with columns \code{state}, \code{month}, \code{month_date},
\code{current}, \code{prior}, \code{change}, \code{pct_growth}.
}
\description{
Computes month-over-same-month-of-prior-year growth rates for each state,
returning both the absolute change and percentage growth.
}
\examples{
# YoY growth for Maharashtra
gst_yoy(state = "Maharashtra")

# National CGST growth, last two years
gst_yoy(component = "cgst", financial_year = c("2023-24", "2024-25"))
}
